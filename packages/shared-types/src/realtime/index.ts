/**
 * The realtime channel's vocabulary (EXPD-023).
 *
 * What a run's channel can say, and who hears each kind of event. The API
 * sends these; the student app (EXPD-047) and Director Mode (EXPD-055)
 * receive them.
 */

export * from './events.ts';
