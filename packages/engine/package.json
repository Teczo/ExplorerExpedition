{
  "name": "@explorer/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Mission Engine — mission types, state, scoring and progression.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build && tsc -p tsconfig.test.json",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@explorer/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
